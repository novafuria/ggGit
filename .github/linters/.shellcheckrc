# ShellCheck configuration for ggGit project
# This file configures ShellCheck linting rules

# Disable specific warnings that are acceptable for our use case
disable=SC2034,SC2086,SC2120,SC2154

# SC2034: Unused variables (acceptable in some utility functions)
# SC2086: Double quote to prevent globbing and word splitting (acceptable for our git commands)
# SC2120: Functions don't take arguments (acceptable for our print functions)
# SC2154: Referenced but not assigned (acceptable in some cases)

# Enable additional checks
enable=all

# Set shell type
shell=bash

# Set severity level
severity=style
